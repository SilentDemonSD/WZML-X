# 🚀 دمج سكريبت الضغط مع بوت تيليجرام - دليل التثبيت والتكوين

## 📋 المحتويات المُضافة

### 1. **ملفات Python الجديدة**

#### `bot/modules/user_compressor_session.py`
- **الوظيفة**: إدارة جلسات المستخدمين والخيارات المختارة
- **الفئات**:
  - `UserCompressionSession`: تخزين خيارات الضغط لكل مستخدم
  - `SessionManager`: إدارة جميع جلسات المستخدمين

#### `bot/modules/compression_handler.py`
- **الوظيفة**: معالج الأوامر والقوائم التفاعلية للضغط
- **الميزات**:
  - عرض قائمة خيارات الضغط قبل التحميل
  - استقبال ملفات الترجمة
  - دمج الخيارات وتمريرها لسكريبت الضغط

#### `encode/orso_compressor_cli.py`
- **الوظيفة**: واجهة سطر أوامر لسكريبت الضغط
- **المميزات**:
  - استدعاء بسيط من البوت
  - دعم جميع خيارات الضغط
  - معالجة الأخطاء والنتائج

---

## 🔧 خطوات التثبيت على Linux Mint

### 1. **متطلبات النظام**
```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Python والمتطلبات
sudo apt install python3 python3-pip ffmpeg ffprobe -y

# تثبيت مكتبات Python المطلوبة
pip3 install pyrogram python-dotenv
```

### 2. **نسخ المشروع من GitHub**
```bash
cd ~/projects  # أو أي مجلد تختاره
git clone https://github.com/inisoliman/orso-leech-compress.git
cd orso-leech-compress
git checkout wzv3  # التبرع لفرع wzv3
```

### 3. **تكوين متغيرات البيئة**

أنشئ ملف `.env` في المجلد الرئيسي:
```bash
cat > .env << EOF
API_ID=YOUR_API_ID
API_HASH=YOUR_API_HASH
BOT_TOKEN=YOUR_BOT_TOKEN
EOF
```

**الحصول على البيانات:**
- **API_ID و API_HASH**: من [my.telegram.org](https://my.telegram.org)
- **BOT_TOKEN**: من [@BotFather](https://t.me/BotFather) على تيليجرام

### 4. **إعطاء صلاحيات التنفيذ**
```bash
chmod +x encode/orso_compressor_cli.py
chmod +x bot/modules/compression_handler.py
```

---

## 🎯 طريقة الاستخدام من التيليجرام

### **الأمر الأساسي:**
```
/l [رابط التحميل] -vt
```

**مثال:**
```
/l https://www.youtube.com/watch?v=dQw4w9WgXcQ -vt
```

### **الخطوات:**

1. **أرسل الأمر** مع الرابط و `-vt`
2. **ستظهر قائمة** بجميع خيارات الضغط:
   - ⚡ ضغط الفيديو
   - 📄 حرق الترجمة
   - 📝 استخراج مسارات
   - 🔊 دمج صوت
   - 📺 تغيير الدقة

3. **اختر الخيارات** التي تريدها بالضغط على أزرار القائمة
4. **إذا اخترت حرق الترجمة:**
   - ستظهر رسالة تطلب ملف الترجمة
   - أرسل ملف الترجمة (SRT/ASS/VTT)
5. **اضغط زر "✅ تم - ابدأ التحميل"**
6. **سيبدأ البوت:**
   - تحميل الفيديو
   - تطبيق خيارات الضغط
   - رفع الملف النهائي

---

## 📂 هيكل المشروع بعد الدمج

```
orso-leech-compress/
├── bot/
│   ├── modules/
│   │   ├── user_compressor_session.py   ✅ جديد
│   │   ├── compression_handler.py        ✅ جديد
│   │   ├── mirror_leech.py
│   │   ├── ... (باقي الملفات)
│   ├── __main__.py
│   └── core/
├── encode/
│   ├── orso_compressor.py               (الأصلي)
│   ├── orso_compressor_cli.py           ✅ جديد
│   └── ...
├── .env                                  ✅ يجب إنشاؤه
├── requirements.txt
└── INTEGRATION_GUIDE.md
```

---

## 🚀 تشغيل البوت

### **الطريقة 1: التشغيل المباشر**
```bash
cd ~/projects/orso-leech-compress
python3 -m bot
```

### **الطريقة 2: باستخدام Screen (للتشغيل في الخلفية)**
```bash
screen -S orso-bot
cd ~/projects/orso-leech-compress
python3 -m bot
# للخروج: Ctrl+A ثم D
```

### **الطريقة 3: كـ Systemd Service (للتشغيل الدائم)**

أنشئ ملف `/etc/systemd/system/orso-bot.service`:
```ini
[Unit]
Description=Orso Telegram Bot
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/home/YOUR_USERNAME/projects/orso-leech-compress
Environment="PATH=/usr/local/bin:/usr/bin:/bin"
ExecStart=/usr/bin/python3 -m bot
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

ثم:
```bash
sudo systemctl daemon-reload
sudo systemctl enable orso-bot
sudo systemctl start orso-bot
sudo systemctl status orso-bot  # للتحقق
```

---

## 🔍 استكشاف الأخطاء

### **الخطأ: "orso_compressor_cli not found"**
```bash
# تأكد من وجود الملف
ls -la encode/orso_compressor_cli.py

# أعطِ صلاحيات التنفيذ
chmod +x encode/orso_compressor_cli.py
```

### **الخطأ: "FFmpeg not found"**
```bash
sudo apt install ffmpeg ffprobe -y
which ffmpeg  # تأكد من التثبيت
```

### **الخطأ: "import error"**
```bash
# أعد تثبيت المكتبات
pip3 install --upgrade pyrogram python-dotenv
```

### **عرض السجلات**
```bash
# إذا كان البوت يعمل على screen
screen -r orso-bot

# إذا كان service
sudo journalctl -u orso-bot -f
```

---

## 💡 ملاحظات مهمة

1. **المساحة المطلوبة:**
   - المشروع نفسه: ~500 MB
   - مساحة عمل للتحميل والمعالجة: 5-10 GB موصى به

2. **الملفات المؤقتة:**
   - يتم حذف ملفات الترجمة المؤقتة تلقائياً بعد المعالجة
   - ملفات التحميل تبقى في `/downloads` أو المجلد المُحدد

3. **الأداء:**
   - استخدم `-scale` لتقليل الدقة وتسريع المعالجة
   - GPU تسريع يقلل وقت المعالجة بنسبة 50-70%

4. **الأمان:**
   - احفظ `.env` بعيداً عن الملفات العامة
   - غيّر `API_ID` و `API_HASH` بشكل منتظم

---

## 📞 الدعم والمشاكل

للمزيد من المساعدة:
- تفقد السجلات: `journalctl -u orso-bot`
- اطلب مساعدة في قنوات المشروع على Telegram
- افتح Issue على GitHub

---

**تم إنشاؤه بواسطة: Comet AI Assistant**
**التاريخ: 2025-11-23**
**الإصدار: 1.0**
